import { widthScale } from "utils/scale";

import { space } from "./space";

export const layout = {
  container: {
    flex: 1,
    backgroundColor: "background",
    px: widthScale(space.rg),
    py: widthScale(space.rg),
  },
};
